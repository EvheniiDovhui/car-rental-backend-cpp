# 🚗 Car Rental System — Backend (C++ OOP Project)

ООП-проєкт, що реалізує логіку системи оренди автомобілів.  
Містить повний набір класів, репозиторіїв, сервісів та базові механізми зберігання даних.

## 🎯 Мета проєкту

Реалізувати навчальний ООП-проєкт відповідно до вимог лабораторної роботи:

- Аналіз предметної області
- Пошук і аналіз подібних рішень
- Визначення основних варіантів використання (Use Cases)
- Опис щонайменше 3 детальних Use Case сценаріїв
- Побудова Use Case діаграми
- Визначення основних сутностей та побудова UML-діаграми класів
- Розробка схеми зберігання даних (контейнери/файли/БД)
- Реалізація проєкту мовою C++ з використанням ООП-підходів

## 📂 Структура проєкту

/include – заголовкові файли (.h) з оголошеннями класів  
/src – реалізація класів (.cpp)  
/data – файли з даними (імітація бази даних)  
/tests – модульні тести (за потреби)  
/docs – UML, Use Case, SRS, діаграми  
Makefile – налаштування збірки  
main.cpp – точка входу в програму  
README.md

## 📚 Основні класи

- **Car** — модель автомобіля (id, марка, модель, рік, ціна за добу, статус доступності)
- **Customer** — клієнт системи (id, імʼя, email, телефон)
- **Reservation** — бронювання (id, carId, customerId, дати оренди, загальна вартість)
- **Payment** — інформація про оплату (id, reservationId, сума, статус)
- **CarRepository** — зберігання та отримання списку автомобілів
- **CustomerRepository** — робота з клієнтами
- **ReservationRepository** — зберігання та керування бронюваннями
- **ReservationService** — бізнес-логіка створення та скасування бронювань
- **PaymentService** — імітація оплати та зміна статусу

Детальна структура класів описана в UML-діаграмі у папці /docs.

## ⚙️ Збірка та запуск

### 1. Збірка проєкту


Або, якщо Makefile налаштований на іншу назву цілі — відповідну команду.

### 2. Запуск програми

./car_rental

(Назва бінарника може відрізнятися залежно від Makefile, наприклад ./app.)

## 🔗 Взаємодія з фронтендом

За бажанням, проєкт може бути доповнений простим HTTP-сервером (наприклад, на базі CrowCpp / Pistache / Boost.Beast), який надає REST API:

Приклад набору ендпоінтів:

- **GET /cars** — повертає список доступних авто
- **POST /reservations** — створює нове бронювання
- **GET /reservations** — повертає список бронювань
- **DELETE /reservations/{id}** — скасування бронювання
- **POST /payments** — імітація оплати

У навчальних цілях можливе також консольне меню замість HTTP API.

## 📄 Документація

У папці `/docs` знаходяться:

- uml-class-diagram.* — UML-діаграма класів
- use-case-diagram.* — Use Case діаграма
- use-cases.md — текстові сценарії Use Case
- srs.md або specification.md — основні вимоги до системи
- db-schema.* — схема зберігання даних (ER/таблиці/структури контейнерів)

## 🧪 Тестування

За необхідності можна додати юніт-тести:

- Використовуючи GoogleTest, або
- Власну просту систему тестів (функції, які перевіряють роботу репозиторіїв та сервісів).

Приклад команди для тестів:

make test
./tests


(Конкретна реалізація залежить від структури проєкту.)
